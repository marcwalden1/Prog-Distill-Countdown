diff --git a/verl/trainer/fsdp_sft_trainer.py b/verl/trainer/fsdp_sft_trainer.py
index ca018fb4..c35a7bec 100644
--- a/verl/trainer/fsdp_sft_trainer.py
+++ b/verl/trainer/fsdp_sft_trainer.py
@@ -171,8 +171,10 @@ class FSDPSFTTrainer:
         # Set pin_memory_device when pin_memory is enabled.
         device_name = get_device_name()
 
+        seed = int(self.config.data.get("seed", 0))
+        torch.manual_seed(seed)
         self.train_sampler = DistributedSampler(
-            self.train_dataset, shuffle=True, num_replicas=world_size, rank=rank, drop_last=True
+            self.train_dataset, shuffle=True, num_replicas=world_size, rank=rank, drop_last=True, seed=seed
         )
         self.train_dataloader = StatefulDataLoader(
             dataset=self.train_dataset,
@@ -330,7 +332,14 @@ class FSDPSFTTrainer:
         log_gpu_memory_usage("After initialize optimizer", logger=logger)
 
         self.steps_per_epoch = len(self.train_dataloader)
-        self.total_steps = self.steps_per_epoch * self.config.trainer.total_epochs
+        # When trainer.total_training_steps is set (our SFT setup uses
+        # total_epochs=9999 + total_training_steps as the real stop), size
+        # the LR scheduler to that. Otherwise fall back to the original
+        # steps_per_epoch * total_epochs behavior.
+        if self.config.trainer.get("total_training_steps", None) is not None:
+            self.total_steps = int(self.config.trainer.total_training_steps)
+        else:
+            self.total_steps = self.steps_per_epoch * self.config.trainer.total_epochs
 
         if self.device_mesh.get_rank() == 0:
             print(
diff --git a/verl/utils/dataset/rl_dataset.py b/verl/utils/dataset/rl_dataset.py
index 2c19385c..dd7223d5 100644
--- a/verl/utils/dataset/rl_dataset.py
+++ b/verl/utils/dataset/rl_dataset.py
@@ -164,7 +164,11 @@ class RLHFDataset(Dataset):
             else:
 
                 def doc2len(doc) -> int:
-                    return len(tokenizer.apply_chat_template(doc[prompt_key], add_generation_prompt=True))
+                    if getattr(tokenizer, "chat_template", None):
+                        return len(tokenizer.apply_chat_template(doc[prompt_key], add_generation_prompt=True))
+                    # Base models with no chat template (e.g. gemma-3-270m): the parquet's
+                    # content is already in the repo's template_type='base' format.
+                    return len(tokenizer("".join(m["content"] for m in doc[prompt_key]))["input_ids"])
 
             dataframe = dataframe.filter(
                 lambda doc: doc2len(doc) <= self.max_prompt_length,
@@ -258,7 +262,12 @@ class RLHFDataset(Dataset):
                 row_dict["multi_modal_inputs"].pop("second_per_grid_ts", None)
 
         else:
-            raw_prompt = self.tokenizer.apply_chat_template(messages, add_generation_prompt=True, tokenize=False)
+            if getattr(self.tokenizer, "chat_template", None):
+                raw_prompt = self.tokenizer.apply_chat_template(messages, add_generation_prompt=True, tokenize=False)
+            else:
+                # Base models with no chat template (e.g. gemma-3-270m): the parquet's
+                # content is already in the repo's template_type='base' format.
+                raw_prompt = "".join(m["content"] for m in messages)
             model_inputs = self.tokenizer(raw_prompt, return_tensors="pt", add_special_tokens=False)
             input_ids = model_inputs.pop("input_ids")
             attention_mask = model_inputs.pop("attention_mask")
diff --git a/verl/utils/dataset/sft_dataset.py b/verl/utils/dataset/sft_dataset.py
index 2aa7b203..c52f82f1 100644
--- a/verl/utils/dataset/sft_dataset.py
+++ b/verl/utils/dataset/sft_dataset.py
@@ -124,7 +124,12 @@ class SFTDataset(Dataset):
         prompt_chat = [{"role": "user", "content": prompt}]
 
         # string
-        prompt_chat_str = tokenizer.apply_chat_template(prompt_chat, add_generation_prompt=True, tokenize=False)
+        if getattr(tokenizer, "chat_template", None):
+            prompt_chat_str = tokenizer.apply_chat_template(prompt_chat, add_generation_prompt=True, tokenize=False)
+        else:
+            # Base models with no chat template (e.g. gemma-3-270m): the parquet's
+            # `prompt` column is already in the repo's template_type='base' format.
+            prompt_chat_str = prompt
         response_chat_str = response + tokenizer.eos_token
 
         # tokenize
diff --git a/verl/utils/fsdp_utils.py b/verl/utils/fsdp_utils.py
index c2a75db7..7fbffb5b 100644
--- a/verl/utils/fsdp_utils.py
+++ b/verl/utils/fsdp_utils.py
@@ -117,10 +117,13 @@ def get_fsdp_wrap_policy(module, config=None, is_lora=False):
         transformer_cls_to_wrap = set()
         for layer_class in fsdp_transformer_layer_cls_to_wrap:
             transformer_cls = get_module_class_from_name(module, layer_class)
-            if transformer_cls is None:
-                raise Exception("Could not find the transformer layer class to wrap in the model.")
-            else:
+            if transformer_cls is not None:
                 transformer_cls_to_wrap.add(transformer_cls)
+        # Some HF model classes (e.g. Gemma3ForCausalLM) declare _no_split_modules
+        # that include vision-tower modules absent from the text-only variant.
+        # Match HF Trainer behavior: skip missing classes, raise only if NONE found.
+        if not transformer_cls_to_wrap:
+            raise Exception("Could not find the transformer layer class to wrap in the model.")
 
         transformer_policy = functools.partial(
             transformer_auto_wrap_policy,
diff --git a/verl/workers/sharding_manager/fsdp_vllm.py b/verl/workers/sharding_manager/fsdp_vllm.py
index c9b163a0..20caa915 100644
--- a/verl/workers/sharding_manager/fsdp_vllm.py
+++ b/verl/workers/sharding_manager/fsdp_vllm.py
@@ -340,5 +340,22 @@ class FSDPVLLMShardingManager(BaseShardingManager):
             )
         )
 
+        # gemma3 normalizer-buffer corruption workaround. vLLM's DummyModelLoader
+        # (used when load_format starts with "dummy") calls initialize_dummy_weights,
+        # which iterates state_dict — including persistent buffers. gemma3's
+        # `normalizer = sqrt(hidden_size)` buffer is registered as persistent in
+        # vLLM 0.8.5, so it gets clobbered to a random ~0 value. load_weights only
+        # writes parameters, so the buffer stays corrupted and embed_tokens(x)*~0
+        # produces token salad. Restore from config. (vLLM-side fix is to register
+        # the buffer with persistent=False; this is the verl-side defense.)
+        if not self.base_sync_done:
+            import torch as _torch
+            for _mod in model.modules():
+                _norm = getattr(_mod, "normalizer", None)
+                if isinstance(_norm, _torch.Tensor) and _norm.numel() == 1:
+                    _cfg = getattr(_mod, "config", None)
+                    if _cfg is not None and hasattr(_cfg, "hidden_size"):
+                        _norm.data.fill_(float(_cfg.hidden_size) ** 0.5)
+
         self.base_sync_done = True
         logger.info(f"vLLM load weights, loaded_params: {len(loaded_params) if loaded_params else -1}")
diff --git a/verl/workers/fsdp_workers.py b/verl/workers/fsdp_workers.py
index 067ac03f..278a0faf 100644
--- a/verl/workers/fsdp_workers.py
+++ b/verl/workers/fsdp_workers.py
@@ -748,13 +748,15 @@ class ActorRolloutRefWorker(Worker, DistProfilerExtension):
 
         assert self._is_rollout
 
+        # Fall back to tokenizer when generation_config exists but the
+        # specific field is None (e.g. gemma-3-270m's generation_config.json
+        # has no eos_token_id; without this fallback, downstream code receives
+        # None and torch.tensor(None) raises "Could not infer dtype of NoneType").
+        _gc_eos = getattr(self.generation_config, "eos_token_id", None) if self.generation_config is not None else None
+        _gc_pad = getattr(self.generation_config, "pad_token_id", None) if self.generation_config is not None else None
         meta_info = {
-            "eos_token_id": self.generation_config.eos_token_id
-            if self.generation_config is not None
-            else self.tokenizer.eos_token_id,
-            "pad_token_id": self.generation_config.pad_token_id
-            if self.generation_config is not None
-            else self.tokenizer.pad_token_id,
+            "eos_token_id": _gc_eos if _gc_eos is not None else self.tokenizer.eos_token_id,
+            "pad_token_id": _gc_pad if _gc_pad is not None else self.tokenizer.pad_token_id,
         }
         prompts.meta_info.update(meta_info)
         timing_generate = {}
